# Скопируйте в backend/.env и вставьте свои ключи. Все опциональны.
# .env в .gitignore — в репозиторий не попадёт.

# github.com/settings/tokens -> Generate new token (classic), без скоупов.
# Лимит GitHub API: 5000 req/час вместо 60 (ф.5).
GITHUB_TOKEN=

# etherscan.io/myapikey (бесплатно). Кошельки команды + smart money (ф.7, ф.11).
ETHERSCAN_API_KEY=

# console.anthropic.com -> API keys. Классификация Discord «по существу vs шум» (ф.12).
ANTHROPIC_API_KEY=
# Для задачи «шум/не шум» хватает Haiku — в ~10 раз дешевле дефолтного opus:
# CLASSIFY_MODEL=claude-haiku-4-5-20251001

# Токен ВАШЕГО Discord-аккаунта для scripts/discord_export.py (ф.12).
# Как достать: web.discord.com -> F12 -> Network -> любой запрос к discord.com/api ->
# заголовок Authorization. ВНИМАНИЕ: экспорт юзер-токеном формально против ToS Discord
# (read-only, риск мал, но не нулевой). Аккаунт должен состоять в серверах пула.
DISCORD_TOKEN=

# cryptopanic.com/developers/api (бесплатный тариф). Доп. новости к GDELT (ф.10).
CRYPTOPANIC_TOKEN=

# defillama.com/pro — платный. Автоматические фандинг-раунды и разлоки
# вместо ручного ввода в projects.yaml (ф.4, ф.7).
DEFILLAMA_API_KEY=

# nansen.ai -> API (план Pro). Smart money на перпах Hyperliquid, потоки бирж и
# свежих кошельков по нативным монетам (ф.11). Кредиты — разовый грант, не месячный,
# поэтому предохранители: минимальный интервал между update и резерв кредитов.
NANSEN_API_KEY=
# Прогон разделён: лёгкая часть (перпы + потоки, 8 кредитов) — основа факторов лесенки,
# полная (+киты и спот, 19) — справочные панели. Обе части сами держат свой ритм,
# поэтому update можно ставить в ежедневный планировщик.
# NANSEN_MIN_INTERVAL_DAYS=1    # ритм лёгкой части (дней)
# NANSEN_FULL_INTERVAL_DAYS=7   # ритм китов и спота (дней)
# NANSEN_MIN_CREDITS=100        # ниже этого остатка коллектор останавливается
# NANSEN_WHALES=1               # 0 = никогда не запрашивать позиции китов
# NANSEN_SPOT=1                 # 0 = никогда не запрашивать спотовую когорту SM
